import styled from 'styled-components';

export const AccentWrapper = styled.div`
  position: relative;
  max-width: 490px;
  margin-bottom: 70px;
`;

export const AccentStar = {
  position: 'absolute',
  left: '-45px',
  top: '-35px',
  right: 'auto',
  bottom: 'auto',
  zIndex: '-1',
};
